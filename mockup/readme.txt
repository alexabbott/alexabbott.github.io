# Grailed Mockup

This is simple landing page based on `mockup.png`. This application was built with HTML, SCSS and Javascript,
and was compiled with Gulp.

## Installation

Run `npm i`

## Usage

Run `gulp`

Visit `http://localhost:3000` in your browser (it should automatically open)

